package main

const (
	// random file name length
	keyLength = 20
	// directory in which image should save temporarily
	imageFolder = "/tmp"
	// screenshut image extention
	fileExtention = "png"

	// Webserver port
	port = "8080"
)
